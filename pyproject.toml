[project]
name = "rust-ml-frameworks-benchmarks"
version = "0.1.0"
description = "Benchmarking suite for Rust ML frameworks with visualization"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "plotly>=5.18.0",
    "pandas>=2.1.0",
    "numpy>=1.24.0",
    "kaleido>=0.2.1",
]
