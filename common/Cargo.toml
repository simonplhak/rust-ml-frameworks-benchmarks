[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
serde = { version = "1.0.228", features = ["derive"] }
criterion = "0.8.2"
rand = "0.8"
csv = "1.3"
burn = { version = "0.20", features = ["dataset", "vision"] }

